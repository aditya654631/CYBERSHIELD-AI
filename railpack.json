{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "variables": {
      "PYTHONPATH": "/app:."
    },
    "startCommand": "python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000} --app-dir ."
  }
}
